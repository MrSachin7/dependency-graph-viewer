---
phase: 05-polish-and-differentiators
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/nodes/ActivityNode.tsx
  - src/components/nodes/ResourceNode.tsx
  - src/hooks/useDagreLayout.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "Each resource node badge shows an emoji prefix followed by the existing abbreviated label (e.g., '💻 TECH', '🔗 3P', '👥 PEOPLE', '🏢 BLDG', '⚙️ EQUIP')"
    - "Each activity node shows the node name on a top row and a compact RTO string on a second row (e.g., '4h RTO', '48h RTO')"
    - "Activity nodes render at h-16 (64px) matching resource node height"
    - "NODE_HEIGHT in useDagreLayout.ts is 64, so dagre rank spacing matches the taller nodes"
    - "Both node components apply transition-opacity duration-150 on their root div, so React Flow opacity changes animate smoothly"
  artifacts:
    - path: "src/components/nodes/ActivityNode.tsx"
      provides: "Two-row activity node with RTO second row and opacity transition"
      contains: "transition-opacity duration-150"
    - path: "src/components/nodes/ResourceNode.tsx"
      provides: "Emoji-prefixed type badge and opacity transition"
      contains: "transition-opacity duration-150"
    - path: "src/hooks/useDagreLayout.ts"
      provides: "Updated NODE_HEIGHT constant"
      contains: "NODE_HEIGHT = 64"
  key_links:
    - from: "src/hooks/useDagreLayout.ts"
      to: "dagre g.setNode"
      via: "NODE_HEIGHT constant"
      pattern: "NODE_HEIGHT"
    - from: "src/components/nodes/ActivityNode.tsx"
      to: "data.rto_hours"
      via: "second row span"
      pattern: "rto_hours"
---

<objective>
Apply three visual enhancements to nodes: emoji-prefixed type badges on resource nodes, an RTO second row on activity nodes, and smooth opacity transitions on both node types.

Purpose: These are the senior differentiators that distinguish the submission — resource type icons (D-01/D-02), RTO on activity nodes (D-03/D-04/D-05), and smooth selection transitions (D-06). All changes are self-contained in the three listed files with no cross-plan dependencies.
Output: Updated ActivityNode, ResourceNode, and useDagreLayout with the visual enhancements in place.
</objective>

<execution_context>
@/Users/sachinbaral/Downloads/dep-graph-viewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sachinbaral/Downloads/dep-graph-viewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-polish-and-differentiators/05-CONTEXT.md
</context>

<interfaces>
<!-- Current code the executor is modifying. Extracted from codebase. -->

From src/components/nodes/ActivityNode.tsx (current state):
```tsx
// Single-row node, h-12 (48px), no transition
const ActivityNode = memo(function ActivityNode({ data, selected }: NodeProps<ActivityFlowNode>) {
  return (
    <div className={`w-40 h-12 bg-white border-2 border-blue-600 rounded-lg flex items-center justify-center px-3 overflow-hidden${selected ? ' ring-2 ring-blue-500' : ''}`}>
      <Handle type="target" position={Position.Top} className="bg-blue-400!" />
      <span className="text-[13px] font-medium text-blue-600 text-center leading-tight truncate w-full">
        {data.name}
      </span>
      <Handle type="source" position={Position.Bottom} className="bg-blue-400!" />
    </div>
  );
});
```

ActivityNodeData has: name: string, rto_hours: number, priority, owner

From src/components/nodes/ResourceNode.tsx (current state):
```tsx
const TYPE_LABELS: Record<ResourceNodeData['resource_type'], string> = {
  technology: 'TECH',
  third_party: '3P',
  people: 'PEOPLE',
  building: 'BLDG',
  equipment: 'EQUIP',
};
// Two-row node, no transition, badge shows TYPE_LABELS text only
```

From src/hooks/useDagreLayout.ts (current state):
```typescript
const NODE_HEIGHT = 48; // uniform height for rank spacing
// Used in: g.setNode(node.id, { width: NODE_WIDTH, height: NODE_HEIGHT })
// Used in: y: y - NODE_HEIGHT / 2
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Update NODE_HEIGHT to 64 in useDagreLayout.ts</name>
  <files>src/hooks/useDagreLayout.ts</files>
  <read_first>
    - src/hooks/useDagreLayout.ts — current file; NODE_HEIGHT is at line 6
  </read_first>
  <action>
Change the `NODE_HEIGHT` constant from `48` to `64`. Update the comment to reflect the new value:

```typescript
const NODE_HEIGHT = 64; // uniform height for rank spacing
```

This is a single-line change. The constant is used in two places: `g.setNode` and the position calculation `y: y - NODE_HEIGHT / 2` — both will pick up the new value automatically.
  </action>
  <verify>
    <automated>grep -n "NODE_HEIGHT" /Users/sachinbaral/Downloads/dep-graph-viewer/src/hooks/useDagreLayout.ts</automated>
  </verify>
  <done>
    - `grep "NODE_HEIGHT = 64" src/hooks/useDagreLayout.ts` returns a match
    - `grep "NODE_HEIGHT = 48" src/hooks/useDagreLayout.ts` returns no match
  </done>
</task>

<task type="auto">
  <name>Task 2: Add emoji prefix to ResourceNode type badge and opacity transition to both nodes</name>
  <files>src/components/nodes/ResourceNode.tsx, src/components/nodes/ActivityNode.tsx</files>
  <read_first>
    - src/components/nodes/ResourceNode.tsx — full file; modifying TYPE_LABELS map and root div
    - src/components/nodes/ActivityNode.tsx — full file; adding second row and changing root div
  </read_first>
  <action>
**ResourceNode.tsx — two changes:**

1. Replace the `TYPE_LABELS` map with a `TYPE_BADGES` map that includes the emoji prefix:

```typescript
const TYPE_BADGES: Record<ResourceNodeData['resource_type'], string> = {
  technology: '💻 TECH',
  third_party: '🔗 3P',
  people: '👥 PEOPLE',
  building: '🏢 BLDG',
  equipment: '⚙️ EQUIP',
};
```

2. Update the badge `<span>` to use `TYPE_BADGES` instead of `TYPE_LABELS`:
```tsx
<span className="self-start text-[10px] font-medium text-gray-500 bg-gray-100 rounded px-1 leading-tight">
  {TYPE_BADGES[data.resource_type]}
</span>
```

3. Add `transition-opacity duration-150` to the root `div` className. The root div currently starts with `relative w-40 bg-white border-2 ...` — add the two Tailwind classes:
```
`relative w-40 bg-white border-2 border-green-600 rounded-md px-3 py-2 flex flex-col gap-1 transition-opacity duration-150${...ring logic...}`
```

**ActivityNode.tsx — three changes:**

1. Change the root `div` from single-row centered layout to a flex-column layout that accommodates two rows. Change `h-12 flex items-center justify-center` to `h-16 flex flex-col items-center justify-center` and add `transition-opacity duration-150`:

```tsx
<div className={`w-40 h-16 bg-white border-2 border-blue-600 rounded-lg flex flex-col items-center justify-center px-3 py-2 overflow-hidden transition-opacity duration-150${selected ? ' ring-2 ring-blue-500' : ''}`}>
```

2. Keep the existing name `<span>` unchanged.

3. Add a second `<span>` below the name for the RTO value. Format: `{data.rto_hours}h RTO`. Use muted, smaller styling that reads as secondary information:

```tsx
<span className="text-[11px] text-blue-400 leading-tight">
  {data.rto_hours}h RTO
</span>
```

The Handles stay in their current positions (target at Top, source at Bottom).

Final ActivityNode JSX structure:
```tsx
<div className={`w-40 h-16 bg-white border-2 border-blue-600 rounded-lg flex flex-col items-center justify-center px-3 py-2 overflow-hidden transition-opacity duration-150${selected ? ' ring-2 ring-blue-500' : ''}`}>
  <Handle type="target" position={Position.Top} className="bg-blue-400!" />
  <span className="text-[13px] font-medium text-blue-600 text-center leading-tight truncate w-full">
    {data.name}
  </span>
  <span className="text-[11px] text-blue-400 leading-tight">
    {data.rto_hours}h RTO
  </span>
  <Handle type="source" position={Position.Bottom} className="bg-blue-400!" />
</div>
```
  </action>
  <verify>
    <automated>cd /Users/sachinbaral/Downloads/dep-graph-viewer && pnpm tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `grep "transition-opacity duration-150" src/components/nodes/ActivityNode.tsx` returns a match
    - `grep "transition-opacity duration-150" src/components/nodes/ResourceNode.tsx` returns a match
    - `grep "TYPE_BADGES" src/components/nodes/ResourceNode.tsx` returns a match
    - `grep "💻 TECH" src/components/nodes/ResourceNode.tsx` returns a match
    - `grep "rto_hours" src/components/nodes/ActivityNode.tsx` returns a match
    - `grep "h-16" src/components/nodes/ActivityNode.tsx` returns a match
    - `pnpm tsc --noEmit` exits 0
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/sachinbaral/Downloads/dep-graph-viewer
pnpm tsc --noEmit
grep "NODE_HEIGHT = 64" src/hooks/useDagreLayout.ts
grep "transition-opacity duration-150" src/components/nodes/ActivityNode.tsx src/components/nodes/ResourceNode.tsx
grep "TYPE_BADGES" src/components/nodes/ResourceNode.tsx
grep "rto_hours" src/components/nodes/ActivityNode.tsx
```

All greps must match. TypeScript must compile cleanly.
</verification>

<success_criteria>
- ResourceNode badge renders emoji + label (e.g., "💻 TECH") — per D-01/D-02
- ActivityNode is 64px tall with name row + RTO row (e.g., "4h RTO") — per D-03/D-04
- NODE_HEIGHT updated from 48 to 64 so dagre spacing is correct — per D-05
- Both node root divs carry transition-opacity duration-150 — per D-06
- Zero TypeScript compile errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-polish-and-differentiators/05-02-SUMMARY.md`
</output>
