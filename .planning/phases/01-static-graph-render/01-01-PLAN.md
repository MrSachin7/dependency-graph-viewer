---
phase: 01-static-graph-render
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
autonomous: true
requirements: [GRAPH-04]

must_haves:
  truths:
    - "@dagrejs/dagre is resolvable as a module in the project"
  artifacts:
    - path: "node_modules/@dagrejs/dagre/package.json"
      provides: "Dagre layout library"
      contains: '"name": "@dagrejs/dagre"'
  key_links:
    - from: "package.json"
      to: "node_modules/@dagrejs/dagre"
      via: "pnpm install"
      pattern: '"@dagrejs/dagre"'
---

<objective>
Install the @dagrejs/dagre dependency that Plan 02 requires for automatic hierarchical node layout.

Purpose: GRAPH-04 requires dagre-based layout. The package is NOT present in node_modules and must be installed before any layout code is written.
Output: @dagrejs/dagre added to package.json dependencies; node_modules populated.
</objective>

<execution_context>
@/Users/sachinbaral/Downloads/dep-graph-viewer/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sachinbaral/Downloads/dep-graph-viewer/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-static-graph-render/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @dagrejs/dagre</name>
  <files>package.json, pnpm-lock.yaml</files>
  <read_first>
    - package.json (verify @dagrejs/dagre is absent before installing)
  </read_first>
  <action>
    Run: pnpm add @dagrejs/dagre

    This installs @dagrejs/dagre@3.0.0 (latest confirmed 2026-03-29) and its peer dependency @dagrejs/graphlib as a transitive dep. Do NOT install the old `dagre` package (unmaintained) — use only @dagrejs/dagre.

    No code changes are needed in this plan. The install is the sole deliverable.
  </action>
  <verify>
    node -e "require('@dagrejs/dagre')" && echo "OK"
  </verify>
  <acceptance_criteria>
    - `grep -r '"@dagrejs/dagre"' package.json` returns a match
    - `node -e "require('@dagrejs/dagre')" && echo OK` prints "OK" without error
    - `ls node_modules/@dagrejs/dagre/package.json` exists
  </acceptance_criteria>
  <done>@dagrejs/dagre is importable from Node.js; package.json lists it as a dependency.</done>
</task>

</tasks>

<verification>
pnpm build exits 0 (Next.js still compiles after the new dep is added)
</verification>

<success_criteria>
`node -e "require('@dagrejs/dagre')" && echo OK` prints OK. `pnpm build` succeeds (no new errors).
</success_criteria>

<output>
After completion, create `.planning/phases/01-static-graph-render/01-01-SUMMARY.md`
</output>
